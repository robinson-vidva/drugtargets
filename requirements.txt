# drugtargets offline pipeline (runs locally / in GitHub Actions ONLY — never on Vercel)
duckdb>=1.1.0
pyarrow>=15.0.0
pandas>=2.0.0
requests>=2.31.0
typer>=0.12.0
tomli>=2.0.1 ; python_version < "3.11"
