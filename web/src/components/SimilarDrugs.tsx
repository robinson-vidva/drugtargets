// Phase 2 placeholder — the full similar-drugs view (signed-cosine ranking with
// concordant/discordant chips and score bars) is implemented in Phase 3.
export function SimilarDrugs(_: { drugId: number }) {
  return null;
}
